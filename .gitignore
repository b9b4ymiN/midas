# Dependencies
node_modules/
.codegraph/

# Scratch / working dirs (per-skill workspaces, eval runs)
.scratch/
*-workspace/
.eval/
evals/
# Docs, plans and specs stay local on purpose — this is a skills repo, and the
# writing that surrounds the work is not part of what ships.
docs/

# Local install lock (machine-specific, not source of truth)
skills-lock.json

# OS
.DS_Store
Thumbs.db
nul

# Editors
.vscode/
.idea/
*.swp

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/
*.egg-info/

# Run data and local research artifacts: pack runs, report sources, source PDFs.
# Large, machine-local, and often not ours to redistribute.
.data/
